<!DOCTYPE html>
<html>
  <head>
    <link rel="icon" href="/favicon-16x16.ico" type="image/x-icon">
    <title><%= content_for(:title) || "FitAIplanner" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", media: 'all', "data-turbo-track": "reload", type: "module" %>
  </head>

  <% if request.path == '/terms' %>
    <!-- 利用規約ページ (/terms) は背景なし -->
    <body>
  <% else %>
    <!-- 他のページは背景あり -->
    <body class="w-full h-screen bg-fixed bg-[url('/dalle_20241230.webp')] bg-no-repeat bg-cover bg-center">
  <% end %>

    <nav class="bg-gray-800 text-white">
  <div class="container mx-auto px-4 flex items-center justify-between">
    <!-- 左側: ロゴ -->
    <div class="text-xl font-bold">
      <%= link_to "FitAIplanner", root_path, class: "hover:text-gray-400" %>
    </div>

    <!-- ハンバーガーボタン -->
    <button id="menu-toggle" class="text-white focus:outline-none">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
      </svg>
    </button>

    <!-- メニューリスト -->
    <ul id="menu" class="hidden flex flex-col items-end space-y-4 absolute top-16 right-0 bg-gray-800 text-white p-4 z-50">
      <li>
        <%= link_to 'ホーム', root_path, class: 'hover:text-gray-400 block py-2' %>
      </li>
      <% if user_signed_in? %>
        <li>
          <%= link_to '機能一覧', mypage_path, class: 'hover:text-gray-400 block py-2' %>
        </li>
        <li>
          <%= link_to '利用規約', terms_path, class: 'hover:text-gray-400 block py-2' %>
        </li>
      <% else %>
        <li>
          <%= link_to '利用規約', terms_path, class: 'hover:text-gray-400 block py-2' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>


    <!-- ローディングインジケータ（最初は非表示） -->
    <div id="loading-indicator"
         class="fixed inset-0 flex items-center justify-center bg-white bg-opacity-50"
         style="display: none;">
      <div class="loader"></div>
    </div>

    <!-- スピナー用CSS -->
    <style>
      .loader {
        border: 8px solid #f3f3f3;
        border-top: 8px solid #3498db;
        border-radius: 50%;
        width: 60px;
        height: 60px;
        animation: spin 1.5s linear infinite;
      }

      @keyframes spin {
        0%   { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
      }
    </style>

    <!-- フラッシュメッセージの表示 -->
    <% flash.each do |key, message| %>
      <% if controller_name == 'training_suggestions' && action_name == 'index' %>
        <!-- 部位検索画面専用のフラッシュメッセージ（下部表示） -->
        <div class="fixed bottom-4 left-1/2 transform -translate-x-1/2 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded shadow-lg">
          <%= message %>
        </div>
      <% else %>
        <!-- 他画面用のフラッシュメッセージ（デフォルト: 上部表示） -->
        <div class="fixed top-4 left-1/2 transform -translate-x-1/2 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded shadow-lg">
          <%= message %>
        </div>
      <% end %>
    <% end %>

    <!-- コンテンツ -->
    <%= yield %>

  </body>
</html>
